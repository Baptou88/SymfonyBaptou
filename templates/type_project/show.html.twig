{% extends 'base.html.twig' %}

{% block title %}TypeProject{% endblock %}

{% block body %}
    <h1>TypeProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_project.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_project_index') }}">back to list</a>

    <a href="{{ path('type_project_edit', {'id': type_project.id}) }}">edit</a>

    {{ include('type_project/_delete_form.html.twig') }}
{% endblock %}
