{% extends 'base.html.twig' %}

{% block title %}TypeProject index{% endblock %}

{% block body %}
    <h1>TypeProject index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_project in type_projects %}
            <tr>
                <td>{{ type_project.id }}</td>
                <td>{{ type_project.name }}</td>
                <td>
                    <a href="{{ path('type_project_show', {'id': type_project.id}) }}">show</a>
                    <a href="{{ path('type_project_edit', {'id': type_project.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_project_new') }}">Create new</a>
{% endblock %}
